/* Enum representing the nodes in the LangGraph. */
export enum GraphNode {
    INITIAL = 'initial',
    PROCESS = 'process',
    DATA = 'data',
}

export interface GlobalFigmaInfo {
    thumbnail: string;
}
