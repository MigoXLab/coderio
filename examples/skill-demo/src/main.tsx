import React from 'react'
import ReactDOM from 'react-dom/client'
import CertoLandingPage from './components/certo-landing-page'
import './globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CertoLandingPage />
  </React.StrictMode>,
)
